# Help Content

This directory hosts the packaged, offline help content for ScratchRobin.

Structure:
- `help_index.json` describes available topics.
- `topics/` holds Markdown content grouped by category.
- `images/` contains screenshots and diagrams referenced by topics.
